<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>

  <!-- This view inherits from the base.view_partner_form view, which is the standard form view for the res.partner model. It adds a new field, max_amount, after the second group element in the form using an xpath expression. -->
    <!-- <record model="ir.ui.view" id="res_partner_view_inherit">
      <field name="name">res.partner.view.inherit</field>
      <field name="model">res.partner</field>
      <field name="inherit_id" ref="base.view_partner_form"/>
      <field name="arch" type="xml">
        <xpath expr="/form/sheet/group[2]" position="after">
          <group>
            <field name="max_amount" string="Maximum allowed amount"/>
          </group>
        </xpath>
      </field>
    </record>


    #The second view inherits from the sale.view_order_form view, which is the standard form view for the sale.order model. It adds a new div element to the form before the footer element using an xpath expression. The div element displays a warning message if the total amount of the sale order exceeds the max_amount field of the partner.
    <record model="ir.ui.view" id="sale_order_view_inherit">
      <field name="name">sale.order.view.inherit</field>
      <field name="model">sale.order</field>
      <field name="inherit_id" ref="sale.view_order_form"/>
      <field name="arch" type="xml">
        <xpath expr="/form/sheet/footer" position="before">
          <div class="alert alert-warning" t-if="order.partner_id.max_amount and sum(line.price_total for line in order.order_line) > order.partner_id.max_amount">
            <i class="fa fa-warning"/>
            The total amount of this sale order exceeds the maximum allowed for the partner.
          </div>
        </xpath>
      </field>
    </record> -->
  </data>
</odoo>

